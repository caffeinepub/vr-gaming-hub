# VRHub

## Current State
Bookings are purely frontend — a random VR-XXXXXX ID is generated and shown on the success screen, but nothing is saved to the backend. The backend has `addBooking`/`getBookings` functions but BookingForm.tsx never calls them. There is no admin dashboard page.

## Requested Changes (Diff)

### Add
- `bookingId` field to the Booking type in the backend
- `deleteBooking(bookingId: Text)` function in the backend to remove a booking by ID
- Admin dashboard page at route `/admin-dashboard` showing all stored bookings in a table (Customer Name, Phone Number, Booking Date, Booking ID) with a Delete button per row
- Frontend routing so `/admin-dashboard` renders the dashboard and `/` renders the homepage

### Modify
- `addBooking` backend function to accept `bookingId` as a parameter and store bookings keyed by bookingId
- `BookingForm.tsx` to call `addBooking` on the backend after generating the booking ID, storing name/phone/date/package/groupSize/message/bookingId
- `backend.d.ts` will be regenerated by generate_motoko_code

### Remove
- Nothing

## Implementation Plan
1. Regenerate Motoko backend with updated Booking type (adds bookingId), updated addBooking signature, and new deleteBooking function
2. Update BookingForm.tsx to call backend.addBooking after generating the local VR-ID
3. Create AdminDashboard.tsx component with table + delete logic using backend.getBookings / backend.deleteBooking
4. Update App.tsx to add react-router-dom routing: `/` → main page, `/admin-dashboard` → AdminDashboard
